#pragma once
#include <math.h>
/* get bit count*/
int countSetBits(unsigned int n);
/* the number of table out of its bit position */
int getTableIndex(int table_bit);
unsigned next_set_of_n_elements(unsigned x);
int binomialeCoeff(int num, int max);


